@model Identity.ViewModels.ProfileFormViewModel
@{
    ViewData["Title"] = "Delete User";
}


<h2>@*Delete an account*@Supprimer un compte</h2>
<h3>@*Are you sure you want to delete this?*@Êtes-vous sûr de vouloir supprimer ceci?</h3>
<hr />
<div>
    <dl class="row mb-3" style=" width: 99%">
        <dt class="col-sm-2 mb-3">
            @*@Html.DisplayNameFor(model => model.FullName)*@Nom prénom
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FullName)
        </dd>
        <dt class="col-sm-2 mb-3">
            @*@Html.DisplayNameFor(model => model.Email)*@Addresse mail
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>
    <form asp-controller="Users" asp-action="Delete" method="post">
        @Html.AntiForgeryToken()

        <input type="hidden" asp-for="Id" />

        <div class="form-group">
            @*<input type="submit" value="Delete" class="btn btn-danger"/>
            <a asp-action="Index" class="btn btn-secondary">Cancel</a>*@
            <input type="submit" value="Supprimer" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Annuler</a>
        </div>
    </form>
</div>